language: node_js

addons:
  homebrew:
    packages:
      - tcpdump
  apt:
    packages:
      - tcpdump
os:
  - linux
  - osx

node_js:
  - node
  - lts/*

cache:
  directories:
    - "node_modules"

script:
  - sudo -E npm test

after_success:
  - sudo -E npm run report-coverage
