language: node_js

sudo: required

addons:
  homebrew:
    packages:
      - tcpdump
  apt:
    packages:
      - tcpdump
os:
  - linux
  - osx

node_js:
  - node
  - lts/*

cache:
  directories:
    - "node_modules"

after_success:
  - npm run report-coverage
