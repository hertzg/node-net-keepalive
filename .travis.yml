language: node_js

addons:
  homebrew:
    packages:
      - tcpdump
  apt:
    packages:
      - tcpdump
os:
  - linux
  - osx

node_js:
  - node
  - lts/*
  - 13
  - 11
  - 10

cache:
  directories:
    - "node_modules"

script:
  - sudo npm test

after_success:
  - npm run report-coverage
